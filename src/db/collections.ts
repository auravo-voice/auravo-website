/** PocketBase collection ids (create these in PocketBase admin if missing). */
export const PB = {
  users: "users",
  practiceSessions: "practice_sessions",
  sessionScores: "session_scores",
  sessionTranscripts: "session_transcripts",
  onboardingBaselines: "onboarding_baselines",
  baselineSegments: "baseline_segments",
  simulationTurns: "simulation_turns",
  baselineHandoffs: "baseline_handoffs",
} as const;
