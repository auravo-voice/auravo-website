# PocketBase (required — set at build AND runtime)
NEXT_PUBLIC_POCKETBASE_URL=https://pb.auravo.ai

# Public app URL for OAuth callbacks (recommended for custom domains)
# NEXT_PUBLIC_APP_URL=https://app.auravo.ai

# Ollama (server-side only)
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5:7b
# AURAVO_COACH_TIMEOUT_MS=180000

# Transcription (self-hosted API; optional on Vercel)
# TRANSCRIPTION_PROVIDER=placeholder
# TRANSCRIPTION_PROVIDER=faster-whisper
# FASTER_WHISPER_MODEL=base
# FASTER_WHISPER_PYTHON=/path/to/.venv-transcription/bin/python
# AURAVO_ALLOW_PLACEHOLDER_FALLBACK=0

# Dev: LAN testing from phone
# NEXT_ALLOWED_DEV_ORIGINS=192.168.1.37
